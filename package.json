{
  "name": "@rafa-thayto/md",
  "version": "1.0.0",
  "description": "A CLI tool for browsing markdown files in a beautiful web interface with real-time updates",
  "license": "MIT",
  "author": "Rafa Thayto",
  "type": "module",
  "bin": {
    "md": "./dist/cli.js"
  },
  "scripts": {
    "dev": "bun run dev:client & bun run dev:server",
    "dev:client": "cd src/client && bun run dev",
    "dev:server": "bun run --watch src/cli.ts",
    "build": "bun run build:client && bun run build:server && bun run build:copy",
    "build:client": "cd src/client && bun run build",
    "build:server": "bun build src/cli.ts --outdir dist --target bun --minify",
    "build:copy": "cp -r src/client/dist dist/client",
    "test": "bun test"
  },
  "dependencies": {
    "chokidar": "^3.5.3",
    "glob": "^10.3.10",
    "open": "^10.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
